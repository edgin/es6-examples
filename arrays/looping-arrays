let arr = [1, 2, 3, 4, 5];

//for each 

//arr.forEach(function callback(currentValue[, index[, array]]) {
    //your iterator
//}[, thisArg]);

arr.forEach((num, index) => {
    console.log(arr[index] = num * 2);
});

// map 

let doubled = arr.map(num =>{
   console.log(num*2);
})
